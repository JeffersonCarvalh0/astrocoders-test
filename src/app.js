import React from 'react';
// import styled from 'styled-components'

import NavBar from './navbar';

const App = props => {
    return (
        <div>
            <NavBar />
        </div>
    );
}

export default App;
